services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=metalSurfaceDetector'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5433:5432'
